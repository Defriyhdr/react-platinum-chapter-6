// import ProtectedRoute from "./ProtectedRoute";

// const CombineRoute = (routList) => {
//   const routes = routList
//     .filter((route) => route.isProtected)
//     .map((item) => <ProtectedRoute>{item}</ProtectedRoute>);
// };

// BELOM SELESAI
